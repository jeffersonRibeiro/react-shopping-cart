import { ICartTotal } from 'models';

const mockTotal: ICartTotal = {
  productQuantity: 1,
  installments: 1,
  totalPrice: 10.9,
  currencyId: 'USD',
  currencyFormat: '$',
};

export default mockTotal;
